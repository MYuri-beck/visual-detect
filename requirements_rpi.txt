# ================================================
# requirements_rpi.txt — Visual Detect (Raspberry Pi 4)
# Instalar APÓS o requirements.txt principal:
#   pip install -r requirements.txt
#   pip install -r requirements_rpi.txt
# ================================================

# Câmera Raspberry Pi (módulo CSI)
# Nota: picamera2 é instalada preferencialmente via apt no Raspberry Pi OS:
#   sudo apt install python3-picamera2
# A linha abaixo serve como fallback via pip (pode não funcionar em todos os sistemas):
# picamera2==0.3.31

# Comunicação serial (ESP32 via USB)
pyserial==3.5

# ================================================
# NOTA: As bibliotecas abaixo são instaladas via
# apt-get no Raspberry Pi OS, NÃO via pip:
#   sudo apt install python3-gi python3-cairo python3-picamera2
# ================================================
# PyGObject (via apt: python3-gi)
# pycairo    (via apt: python3-cairo)
