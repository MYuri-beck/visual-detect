# ================================================
# requirements_rpi.txt — Visual Detect (Raspberry Pi 4)
# Instalar APOS o requirements_pc.txt principal:
#   pip install -r requirements_pc.txt
#   pip install -r requirements_rpi.txt
# ================================================

# Camera Raspberry Pi (modulo CSI)
# Nota: picamera2 e instalada preferencialmente via apt no Raspberry Pi OS:
#   sudo apt install python3-picamera2
# A linha abaixo serve como fallback via pip (pode nao funcionar em todos os sistemas):
# picamera2==0.3.31

# Comunicacao serial (ESP32 via USB)
pyserial==3.5

# ================================================
# NOTA: As bibliotecas abaixo sao instaladas via
# apt-get no Raspberry Pi OS, NAO via pip:
#   sudo apt install python3-gi python3-cairo python3-picamera2
# ================================================
# PyGObject (via apt: python3-gi)
# pycairo    (via apt: python3-cairo)
